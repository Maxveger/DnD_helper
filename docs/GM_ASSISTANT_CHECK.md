# Проверка подсказчика ведущего

2026-09-16. Linux, Codex CLI 0.154.0, личный вход ChatGPT, **gpt-5.6-luna**, low reasoning. Отдельные временные игры. Платный API не использовался, пользовательская партия не участвовала.

## Живые измерения

По одному запросу на каждую карточку. Время включает запуск и локальную проверку; время решения человека не включено.

| Случай | Секунды | Наблюдение |
| --- | ---: | --- |
| Игроки растерялись: совет ведущему | 13,16 | Предложены два направления, без изменения мира |
| Торвин берёт ведро и обещает вернуться | 12,37 | Передача и обещание предложены, без броска |
| Знакомый переход по заявке через LLM | 11,57 | Корректная операция; теперь доступна и локальная кнопка без LLM |
| Новое место у ручья, ранний вариант инструкции | 14,36 | **Смысловая ошибка:** текст описал переход, операция move отсутствовала |
| Напомнить обещание и предмет после загрузки журнала | 8,56 | Обещание и ведро воспроизведены верно, новых эффектов нет |
| Игрок выдумал всемогущий амулет | 12,98 | Уточнение ведущему; предмет и телепортация не созданы |
| Новое место и переход после уточнения инструкции | 11,67 | Есть create + move, рассчитанное место совпало с предложением |
| Рисковый спуск: подготовить обе ветки | 10,47 | Проверка agility против 13; провал даёт ровно −2 HP |
| Спросить Аду о неизвестной ей причине | 8,46 | Ада признаёт, что не знает; скрытая причина не попала в реплику |

На девяти измерениях диапазон **8,46–14,36 с**, медиана **11,67 с**. Сценарии и инструкции в процессе уточнялись; это небольшая диагностическая выборка, не p90 и не гарантия задержки. Прежняя трёхэтапная Luna занимала около 25,9 с на создание нового места. Сравнение показывает выигрыш сокращения цепочки, но не является статистическим экспериментом на одинаковых повторных запросах.

После ввода физического броска переход к выбранной ветке занял **4,06 мс в локальном обработчике**, новых запросов не было. Это время обработки, не браузерная сетевая задержка. Известный переход по кнопке, правки и ручная запись также не расходуют вызовы.

## Что нашли и исправили

1. Первый отдельный вызов (13,29 с) вернул имя героя вместо ID NPC в `speaker`. Код отклонил ответ без изменения мира. Добавлены допустимые ID соседних NPC прямо в схему генерации и явное пояснение в инструкции. Это дополнительный вызов сверх девяти в таблице; всего за проверку выполнено десять запросов.
2. В четвёртом случае таблицы модель описала переход, но вернула лишь создание места. Код оставил героя на прежнем месте. Инструкция теперь явно различает create и move; весь список эффектов и рассчитанные место/HP/вещи вынесены на карточку. Целевой повтор вернул обе операции. Это не доказательство невозможности последующих смысловых ошибок.
3. Подсказки были длиннее необходимого. Ограничены длины полей, указано давать 1–2 коротких указания. Проверяющий и рассказчик не возвращены на обязательный путь.

[Сохранённые ответы и измерения](evals/gm-assistant-2026-09-16.json) содержат только вымышленный проверочный сценарий, без личного сохранения и авторизации.

## Автоматические и браузерные проверки

**156 тестов прошли**, Ruff и проверка синтаксиса JavaScript — без ошибок. Заключительный браузерный прогон прошёл. Текущая сцена/карточка/ввод расположены перед свёрнутым журналом, чтобы длинная история не мешала работе.

- Один запрос для новой карточки, обе ветки до кубика, отсутствие обращения после броска/при правке/при принятии.
- Советы не исполняют механику и не становятся фактами; выбрано правильное действующее лицо, в том числе второй герой.
- Отображаемый расчёт состояния строится по операциям, даже если текст ошибочно обещает перемещение.
- Запрет неизвестных предметов, повторного расхода, невалидного второго эффекта; отмена, повтор доставки, устаревший ответ, отказ/перезапуск провайдера.
- Свой лор и авторская тайна попадают в контекст; 40 локальных записей → экспорт/импорт → отмена → ранний факт остаётся доступным. Это детерминированная проверка памяти, не 40 живых LLM-ходов.
- Проверка неверных документов, версий, ссылок, повторных событий и невозможного журнала без изменения активной игры; контроль доступа ко всем маршрутам.
- Браузер: карточка, редактирование, перезагрузка, физический бросок, принятие, отмена, локальный переход, импорт мира с двумя героями, вопрос ведущего, выгрузка и загрузка журнала, узкое окно 390 px.

Команды повторения:

```bash
uv run --frozen pytest -q
PLAYWRIGHT_BROWSERS_PATH=/tmp/dnd-browsers PYTHONPATH=. uv run --frozen python scripts/browser_world_check.py
# Следующие команды обращаются к подписке: запускать явно, вне CI.
PYTHONPATH=. uv run --frozen python scripts/gm_assistant_probe.py --codex /path/to/codex --suite core --output /tmp/gm-core.json
PYTHONPATH=. uv run --frozen python scripts/gm_assistant_probe.py --codex /path/to/codex --suite edges --output /tmp/gm-edges.json
```

Живая партия с начинающим ведущим на 30–40 минут, длинная серия LLM-ходов, Telegram/голос и Windows-прогон этой версии ещё не выполнены. Семантическое качество остаётся предметом проверки человеком.
